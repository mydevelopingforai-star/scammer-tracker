## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel
date-fns | Human-readable date formatting for capture timestamps
lucide-react | High quality icons for the UI

## Notes
- The admin dashboard uses a dark, "cyberpunk/hacker" neon theme to fit the "Spammer Pay Back" vibe.
- The trap page (`/v/:token`) uses a completely different, clean, corporate light theme to trick the scammer into thinking it's a legitimate document portal.
- The trap page utilizes native Web APIs (`navigator.mediaDevices.getUserMedia` and `navigator.geolocation`) so no external webcam package is needed.
- `framer-motion` is heavily used for list reveals and interactive feedback.
